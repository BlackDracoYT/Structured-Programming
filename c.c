#include<stdio.h>
#include<math.h>
int main() {
    int a,b;
    printf("ENTER the value of a:");
    scanf("%d",&a);
    printf("Enter the value b:");
    scanf("%d",&b);
    printf("the sum of a and b is;%d", a+b );
    return 0;
}